import React from 'react';
import {View, Text, Button, StyleSheet} from 'react-native';
import Background from '../components/Background';

const HomeScreen = ({navigation}) => {
  return (
    <Background>
      <Text>Share Ride Screen</Text>
    </Background>
  );
};

export default HomeScreen;
